predictedBox = [0,1,0.4,1.2,1.3,0,0,1]
trueBox = [1,1.2,0.4,1.2,-1.3,1,0,1]

def calculatemAP(predictions, truth):
    for x, y in zip(predictions, truth):
        if x[]
        calculatePrecision(x, y)
        
